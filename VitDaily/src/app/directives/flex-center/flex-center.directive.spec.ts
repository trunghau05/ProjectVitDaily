import { FlexCenterDirective } from './flex-center.directive';

describe('FlexCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new FlexCenterDirective();
    expect(directive).toBeTruthy();
  });
});
